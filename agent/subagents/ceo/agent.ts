import { defineAgent } from "eve";

export default defineAgent({
  description:
    "Executive overview: business health checks, revenue forecasting, goal tracking, morning/weekly strategy reports, and cross-department performance reviews.",
  model: "anthropic/claude-sonnet-5",
});
