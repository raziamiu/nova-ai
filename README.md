# nova-ai

An [eve](https://github.com/vercel/eve) agent project.

## Getting started

```bash
npm install
npm exec -- eve dev
```

See `AGENTS.md` and the docs bundled in `node_modules/eve/docs/` for how to configure
the agent's instructions, tools, channels, and connections.
