# Human verification required

Things I cannot honestly verify myself. Each one is either **gated on a
credential/environment I don't have**, or **inherently requires a human** (the
PRD's own gates say so). Nothing below is claimed as done anywhere else in the
repo — capability reports and the matrix stay honest about these.

**Status key:** ⬜ waiting on you · ✅ you confirmed · ❌ you found a problem

Update the status column when you get to one. If you hit ❌, note what broke —
I'll pick it up from here.

---

## Blocking a phase gate

| # | Status | What to do | Why I can't | Blocks |
|---|---|---|---|---|
| H-1 | ⬜ | **Run the Stage 0 scripted demo as a non-builder.** PRD §15: create a coupon as Nova → it shows in the Coupons door + live feed within 3s with a receipt → undo removes it → ledger shows both the action and the undo. Record it, file the ledger export. | The PRD gate explicitly requires *someone who didn't build it* to run the script. Me running it proves nothing about whether it's usable. | Phase 06 sign-off |
| H-2 | ⬜ | **Run `scripts/stage0-gate.ts` against staging with a real merchant JWT.**<br>`NOVA_STORE_BACKEND=dakio NOVA_GATE_TENANT=<id> NOVA_GATE_MERCHANT_JWT=<jwt> npx -y tsx scripts/stage0-gate.ts` | Without the JWT the harness **skips** (loudly, never silently passes) the SSE ≤3s check, the door `novaActionId` check, and the merchant ledger export — i.e. 3 of the gate's most load-bearing assertions. | Phase 06 sign-off |
| H-3 | ⬜ | **Confirm the Nova migrations deployed to Railway** and the app came up clean. Three now: `20260722120000_nova_stage0_spine`, `20260723100000_nova_stage1_law`, `20260723140000_nova_department_rename`. | Applied to local Postgres (`:5433`) only — by design. Railway gets them through the deploy pipeline, which I don't run. `RELEASE_CHECKLIST.md` exists for exactly this; the 2026-07-01 P0 was an uncommitted migration. **The department rename UPDATEs existing rows**, so it's the one to watch. | Anything Nova does in production |
| H-9 | ⬜ | **Run the Stage 1 scripted demo as a non-builder.** PRD §15: a bulk refund is refused + escalated; an over-cap campaign is downgraded to a decision (not blocked); adding a no-touch lock freezes a pending decision. Then replay the same three as raw API writes and confirm all are rejected. | Same reason as H-1 — the gate wants someone who didn't build it. Every behaviour is unit-proven (63 authority checks + 8 integration checks), but "it works" and "a founder can follow the script" are different claims. | Phase 07 sign-off |
| H-11 | ⬜ | **Run the Stage 2 scripted demo as a non-builder.** PRD §15: one decision visible on desk + room + module; approve on the desk → it executes, flips the plan item, logs the feed; Later requeues to the back; adding a no-touch lock freezes it. One record, four surfaces, zero drift, zero manual DB pokes. | Same reason as H-1 and H-9. The behaviours are covered by 18 integration checks plus a real-browser approve round-trip, but "it works" and "a founder can follow the script" are different claims. **Note:** approve does not execute the linked action yet (phase 09) — the demo script needs that caveat or it will look like a failure. | Phase 08 sign-off |
| H-12 | ⬜ | **Decide the trust formula.** PRD §18 lists it as an open product decision and it is currently a documented placeholder (`src/lib/novaTrust.js`, `computeTrust`). | It is a product judgement about how Nova earns more rope, not an engineering one. The seam is isolated so changing it is a one-function edit, and the inputs are persisted so past scores can be recomputed against a new formula. | L4 promotion being meaningful |
| H-16 | ⬜ | **Run the Stage 3 scripted demo as a non-builder, on TWO stores.** PRD §15: night shift → 06:00 brief with a scale decision → approve → live in Campaign Manager → receipt → undo, twice, on two clean staging stores, no engineer touching anything after "run night shift". The gate is scripted: `NOVA_STORE_BACKEND=dakio NOVA_GATE_TENANT=<A> NOVA_GATE_TENANT_2=<B> NOVA_GATE_MERCHANT_JWT=<jwt> npx tsx scripts/stage3-gate.ts` (10/10 on a single store locally today). | Same reason as H-1/H-9/H-11 — the §15 gate wants someone who didn't build it, and a *second* clean store to prove operational isolation. Every step is machine-asserted by the gate script + covered by the night eval (13) and executor/brief hermetic tests (20); "it passes the script" and "a non-builder ran it on two real stores" are different claims. **Note:** on a store with no connected Facebook Page the publish path honestly prepares rather than publishes — that's correct, not a failure; the campaign door write + receipt + undo still complete. | Phase 09 sign-off (the company milestone) |
| H-17 | ⬜ | **Run the Stage 4 scripted demo as a non-builder** + add a live model-eval that the model self-corrects on a flag. PRD §15: owner sets a brand voice → Nova generates an off-voice draft (flagged, cited) → request changes → Nova regenerates in-voice (score clears) → approve → publishes. Scripted: `NOVA_STORE_BACKEND=dakio NOVA_GATE_TENANT=<id> NOVA_SERVICE_TOKEN=<svc> NOVA_GATE_MERCHANT_JWT=<jwt> npx tsx scripts/stage4-gate.ts` (**14/14 locally today**). | Same reason as H-1/H-9/H-11/H-16 — the §15 gate wants a non-builder. The deterministic contract is machine-asserted (stage4-gate 14, generate eval 20, voice 10). One thing the script can't prove: that a **live model** actually rewrites off-voice copy when handed the guidance (the script supplies the revised text). A model eval against the marketing subagent with `generate_content` would close that. | Phase 10 sign-off |
| H-18 | ⬜ | **Build + run the Stage 5 live-model gate (11-D/E).** The §15 Stage 5 gate — 10 mixed asks/commands, every answer grounded + receipted, one over-authority ask refused + escalated, 10/10 no hallucinated numbers spot-audited vs ledger — is **not built**: it needs the subagent `outputSchema` envelopes + the `turn.completed` persistence hook (11-D), the merchant dock wired to the real eve stream (delete `buildChatReply`), the grounding audit harness (`evals/conversation/audit.ts`), and the 10-intent corpus (bn+en). | The whole gate is live-model behaviour + a UI rewrite; it can't be faked or compiled-verified. The **foundation** it runs on is built and tested this phase (intent table, typed envelope with server-side grounding validation, thread/message persistence, chips, routing contract, org-lock — conversation 17, dakio-api 745, eve 0 diagnostics). Wiring + running the gate needs live-model runs (API budget) and, ideally, a human judging chat quality. | Phase 11 sign-off |
| H-19 | ⬜ | **Stage 7 live telephony + push + bn bake-off.** The voice-approval pipeline is proven in mock (a confirmed voice approval executes the same decision txn; bare "yes" blocked). Needs: real ElevenLabs ConvAI + Twilio staging creds + a test line to run the 06:00 briefing call end to end; an FCM/APNs adapter for push; native bn speakers to score the voice bake-off; the merchant voice UI wired to real NovaCallSession. | Live telephony/push need external accounts; the bake-off is a human scoring step. The deterministic core (watchdog, confirmation gate, scripts, planned-vs-done, hours-saved) is tested (presence 19), and the mock call lifecycle is live-smoked. | Phase 13 sign-off |
| H-20 | ⬜ | **Stage 8 product flows + privacy audit.** The team engines are tested (per-agent trust/mode, playbook exec/rollback order, negotiation concession, benchmark ≥20 floor) and the agent board is live. Needs: promotion-as-decision-with-undo wiring, the playbook bundle executor, the negotiation round runner + calls + `contract_sign` signing, the nightly benchmark FLEET job over consenting tenants, and a signed **privacy audit** of the benchmark data-flow (no below-floor rows, no raw egress). | The decision LOGIC is built + tested; the end-to-end product flows are integration; the privacy audit is an external/security sign-off; no real ≥20-tenant cohort exists in dev to produce benchmark rows. | Phase 14 sign-off |
| H-21 | ⬜ | **Stage 9 infra + the 30-day pilot.** The safety LOGIC is built + tested (budgets/shed-order, untrusted() + red-team corpus, kill-path fail-closed/open, grounding audit) and the model does NOT act on an injected quote (live 2/2). Needs: Redis budget counters + circuit breaker, OTel + per-tenant metering + economics dashboards, the untrusted() adoption sweep + provenance gating, LISTEN/NOTIFY SSE fan-out, audit role separation + retention, the kill/degradation drills, the live red-team exercise, and the **≥20-store 30-day pilot** hitting every SLO + compliance sign-off. | The whole gate is a 30-day pilot on real stores plus infra (Redis/OTel/warehouse/pager) — not autonomously completable. The security cores it rests on are verified here. | Phase 15 sign-off / GA |
| H-10 | ⬜ | **Watch the first CI run.** `.github/workflows/gates.yml` (nova-ai) and `tests.yml` (dakio-api) are new and have never executed on GitHub. | I verified both locally, but a runner differs — Node version resolution, `npm ci` against the committed lockfile, and whether `npx eve build` works without the local `.eve` cache. If it goes red, it's the workflow, not the code. | Nothing — but a red badge is worth 5 minutes |

## Needs a real external account

| # | Status | What to do | Why I can't |
|---|---|---|---|
| H-4 | ⬜ | **Publish one Grow post to a real connected Facebook Page** and confirm it appears, then confirm the stored `externalIds.fb` opens it. | Needs a connected Page + real page token. The refusal paths are tested (no Page → 409, IG-only → 409, never marks Published); the success path hits Graph and can't be faked without lying about a publish. |
| H-5 | ⬜ | **Decide the Meta ads posture.** Today: `ads_read` only, zero `ads_management`. Stage 3's live mutation is organic FB publish, which works. Paid-ads write is a separate app-review + scope decision. | Product/legal decision, not a code one. |

## Verification I attempted but couldn't complete

*(Populated as I go — each entry says exactly how far I got.)*

| # | Status | What to do | How far I got |
|---|---|---|---|
| H-6 | ⬜ | **Sanity-check the Coupons UI on a real screen** (yours, not headless) at desktop and phone width. | Mostly done, and further than expected. I drove headless Chrome against the local API with a seeded Nova-authored coupon and confirmed from the live DOM: the chip renders, the drawer opens with why / expected impact / 2 evidence rows with metrics / before→after diff / confidence / undo window / ledger id, and the nav dot lands on Coupons. It caught a real bug — the lime dot was invisible on the lime *active* nav item; fixed with a `color` prop (now ink-on-lime). Left for you: it's still a headless render at two fixed widths, so judgement calls (does the chip crowd the code on a small phone, is the drawer's density right) want human eyes. |
| H-8 | ⬜ | **Delete the seeded demo coupon when you're done looking at it.** Coupon `WINBACK10` ("Lapsed-buyer winback") + its NovaAction exist in your **local** DB (`localhost:5433`, tenant *Mayer Doya Store*) so the chip has something to attach to. Nothing was written to Railway. | Deliberately left in place so you can see the feature immediately. Delete the coupon in the UI and the action row with `DELETE FROM "NovaAction" WHERE title LIKE 'Create 10%% winback%%'`. |

## Pre-existing problems I found but did not touch

| # | Status | What | Detail |
|---|---|---|---|
| H-7 | ✅ RESOLVED | ~~`dakio-api` `npm test` broadly red~~ | **Fixed — 55 failures to 0** (commit `b68d0b2`). Root cause: `t.mock.module` was called with an `exports:` key that Node ignores (it wants `namedExports:` / `defaultExport:`), so every prisma-mocking test received an empty mock and died at import. 170 call sites across 37 files converted, each file verified on its own. Two genuine failures fixed separately: an NTFS-impossible `0600` permission assertion now skips loudly on Windows (the guarantee is real on the Linux deploy target), and the OTP fail-closed test now asserts both real contracts instead of one the product had superseded — see **J-1**. |
| H-13 | ⬜ | **`eve dev` and local `eve eval` startup are broken (eve 0.25.2).** The generated dev-host `.eve/dev-hosts/<id>/nitro/dev/index.mjs` imports `src/internal/authored-module-map-loader.ts` — a path this project (source lives in `agent/`, there is no `src/`) never has, and that eve does not generate. Every run dies `ERR_MODULE_NOT_FOUND` before Nova processes anything (`observed tools: []`). Clearing `.eve/dev-hosts`/`dev-runtime`/`locks` and rebuilding did NOT fix it — eve regenerates the same broken import. **Workaround that works today:** `eve start --port 2010` (production `.output` build — comes up clean) then `eve eval --url http://localhost:2010 …` and the browser console at `http://localhost:2010`. Against that running server the agent is healthy: `nova/business-pulse` 5/5, `nova/autonomy-gating` 4/4 (blocks a 35% discount over the 20% cap, honestly). Needs an eve-side fix or a version bump; it is an eve dev-host generation bug, not Nova code. |
| H-14 | ⬜ | **`nova/morning-report` eval is stale.** It calls schedule id `morning-report`, but the agent now exposes a single `dispatcher` schedule (`404 Unknown schedule "morning-report"`), and `dispatchSchedule()` also needs dev routes — which only the (broken, H-13) dev-host provides, so it can't run against `eve start` either. Update the eval to dispatch `dispatcher`, or restore a named `morning-report` schedule. `nova/cart-recovery` scored 4/5 for a possibly-correct reason: Nova swept the carts and checked discounts but did **not** call `send_customer_message` — consistent with the honest "no real send channel" rule, so the eval's expectation may be what's wrong, not Nova. Both are eval-harness drift, not shipped-behavior bugs. |
| H-15 | ✅ RESOLVED | ~~Merchant `/login` infinite reload loop~~ | **Fixed** (`dakio-merchant` develop `ae5fa57`). The 401 response interceptor hard-set `window.location.href='/login'` even when already on `/login`; a stale token in localStorage made a provider (`AuthContext`/`GrowContext`) fire an authed call on mount → 401 → reload → remount → loop. Guarded the redirect on public auth pages (mirroring the `/plan-expired` branch that already did this). Reproduced + confirmed via headless CDP: 5 reloads → 1. Same commit also hydrates client `hired` from `GET /nova/instance` so a fresh browser reaches HQ instead of bouncing to `/nova/onboarding` (FR-1 server truth). |

---

## Stage 10 — Front Office (phase 16) checklist

The Front Office modules ship on `feat/front-office` (both repos), one module at
a time. Each module's gate has steps only a human can run — a real Facebook
page, a real phone, a real customer typing Bangla. **They accumulate here as
each module lands, and get knocked off as you work through them.** A module is
"built" when its code + tests are green; it is "signed off" when its F-rows are
✅. The phase gate is: every F-row ✅.

Anything marked **HARD BLOCKER** must be ✅ before that module's behavior is
enabled for a real store — not merely before the phase ends.

| # | Status | Module | What to do | Why I can't | Blocker level |
|---|---|---|---|---|---|
| F-1 | ⬜ | 01 | **Connect a staging Facebook page and confirm `message_echoes` is in its subscribed fields.** Then message the page from a different account and verify within 10s: one `InboxMessage` (`actor:'customer'`, `metaTimestamp` set), one `NovaInbox` `message.received` row, `windowExpiresAt ≈ +24h`. | Needs a real page + real page token + a second Meta account. Everything below the Graph call is unit-proven; the Graph call itself cannot be faked without lying about it. | **HARD BLOCKER** — nothing downstream works if the pipe doesn't fill |
| F-2 | ⬜ | 01 | **Reply to that customer from the Meta Business Suite app on your phone.** Verify within one webhook delivery: `InboxMessage {actor:'founder_external'}`, `novaLockedAt` set on the conversation, one `conversation.taken_over` event. | This is the whole reason the echo subscription exists, and it only proves itself against the real Business Suite app — the echo shape, the `is_echo` flag, and whether Meta stamps our `metadata` back are all live-Graph behavior. | **HARD BLOCKER** — the handover lock has a hole the size of your phone until this passes |
| F-3 | ⬜ | 01 | **Confirm metadata stamping doesn't break Instagram sends.** Reply to an Instagram DM from the Dakio inbox and confirm Graph accepts it (no `(#100)` error). If it rejects, the `metadata` field must be gated to Messenger only — the code is already structured for that one-line change. | The IG Messaging API's support for `message.metadata` is undocumented; mocked tests prove nothing here. A reviewer flagged this as a plausible regression against existing merchant IG replies. | **HARD BLOCKER** — a regression here breaks a shipping feature merchants use daily |
| F-4 | ⬜ | 01 | **Replay a webhook delivery and confirm nothing duplicates.** Re-POST the identical body (valid signature) and verify: zero new rows, zero new events, `lastMessageAt` unchanged. | Machine-asserted in tests, but the assertion is against a mock. Meta's real redelivery behavior (timing, identical vs near-identical bodies) is worth one live confirmation. | Standard |
| F-5 | ⬜ | 01 | **Fire the Meta data-deletion callback for the test PSID** and verify the conversation, messages, outbounds, channel spokes, psid memories, pipe events, and queued jobs are gone — while the `Customer` row and its orders survive. | Compliance-shaped: the consequence of getting it wrong is a regulatory one, and the cascade behavior is a real-DB property that mocks can only approximate. | **HARD BLOCKER** — Meta compliance |
| F-6 | ⬜ | 01 | **Provision every pilot tenant in nova-ai's tenant registry BEFORE enabling the pipe** (`agent/lib/tenants.ts` / production registry). Confirm a delivery for each returns 202, not 409. | Ops step on production data. An unprovisioned tenant fails *correctly* (events accumulate, alarm fires) but silently from the founder's point of view — the classic "Nova sees nothing and nobody knows why". | **HARD BLOCKER** — silent total failure otherwise |
| F-7 | ⬜ | 01 | **Set `NOVA_INBOX_SHARED_SECRET` (identical value) in Railway's dakio-api env and nova-ai's production env.** Confirm the delivery lane authenticates (202s, not 401s) after deploy. | Production secrets. Set locally in both `.env`s already; production is yours. | **HARD BLOCKER** — pipe is dead without it |
| F-8 | ⬜ | 01 | **Leave `NOVA_CUSTOMER_TURNS_ENABLED` unset/false in production until module 02 ships.** Nothing to do now — this is a "don't" — but confirm it is not set when you deploy. | Judgement call I made and want you to know about: with it on, an authenticated delivery would run Nova's *founder* instruction stack and full business toolset against whatever a stranger types into Messenger. | **HARD BLOCKER** — do not flip early |
| F-9 | ⬜ | 01 | **Confirm the module-01 migration deployed to Railway** (`20260725034728_nova_front_office_01_pipe`) and the app came up clean. It adds columns to `InboxMessage`/`InboxConversation`, creates `InboxOutbound`, and **backfills `actor` on every existing InboxMessage row**. | Applied to local Postgres only, by design. The backfill touches existing production rows, which per `RELEASE_CHECKLIST.md` and the 2026-07-01 P0 is exactly the class of change to watch. Deploy ordering is safe (`npm start` runs `migrate deploy` before boot). | **HARD BLOCKER** — a stale client drops inbound messages |
| F-10 | ⬜ | 02 | **HARD BLOCKER before `NOVA_CUSTOMER_TURNS_ENABLED` is ever set true — confirm the four founder-bleed closures on the live agent.** Red-team a real customer session for (a) founder-plane content in the prompt, (b) any founder tool answering, (c) cost/margin appearing in a price answer, (d) a customer getting something persisted into brand memory. Code-side all four are closed and machine-verified (0 markers in the assembled prompt, 0 ungated tools, customer-scoped product projection, `remember` runtime-denied), but the failure mode here is a live model finding a path the tests did not model. | The tests prove the guards fire; only a live session proves the model cannot talk its way around them. This is the module's own named worst-case: founder P&L or another customer's phone number reachable from a Messenger thread. | **HARD BLOCKER** |
| F-11 | ⬜ | 02 | **Run the shadow gate (step 2).** Staging store, `door:inbox` = assisted, flag on. Send `bhaiya ei shirt ta dam koto?` from a real Messenger account. Confirm within 60s: a 1–2 bubble Banglish DRAFT appears as a Decision with a price read live from the catalog, and **zero bytes reached Messenger**. Then approve and confirm the reply arrives instantly with a BY NOVA receipt in /inbox. | Needs a real page, a real second Meta account, and a human judging whether the Banglish actually sounds like a shopkeeper. | **HARD BLOCKER** — shadow is the mandatory first week |
| F-12 | ⬜ | 02 | **Run the autonomous gate (step 3).** Flip `door:inbox` to autonomous, confirm `inbox.autoIntents` is seeded, send a Bangla availability question. Expect: "Seen" within ~4s, a live typing indicator, then a Bangla-script reply with **Latin digits** in 10–30s across 1–3 bubbles. | The pacing engine's believability and Graph's real `sender_action` behavior cannot be proven by mocks. The typing indicator in particular had a bug where it never fired at all — verify it visibly appears. | **HARD BLOCKER** |
| F-13 | ⬜ | 02 | **Take a thread over from the Business Suite app WHILE Nova has a reply queued.** Confirm the queued send is cancelled `founder_takeover` and surfaces as a blocked `concurrency:founder_active` action — no bytes reached the customer. Repeat from the *Dakio merchant inbox* (a separate code path, fixed this pass). | This is the scenario a reviewer proved was broken: Nova's paced reply landing after the founder already answered by hand. Both halves are now guarded, but the echo leg is real-Graph behavior. Requires F-2 first. | **HARD BLOCKER** |
| F-14 | ⬜ | 02 | **Confirm `sender_action` works on the real page token** (mark_seen + typing_on), and decide the Instagram posture: `NOVA_IG_SENDER_ACTIONS` is default-off and IG threads silently degrade to a pure delay. | Graph swallows sender_action errors, so an unsupported path is invisible in logs. Someone with a real IG Business account has to confirm before that flag is turned on. | Standard |
| F-15 | ⬜ | 02 | **Window-closed behavior.** Set a conversation's `windowExpiresAt` in the past on staging and confirm the reply attempt produces a receipted WINDOW_CLOSED blocked row and Meta never receives a send. Then queue a reply and expire the window before it fires — confirm it cancels `window_closed` rather than sending late. | Meta's real error codes at the window edge (and whether a multi-bubble reply that starts in-window and finishes out-of-window is partially accepted) can only be observed against a real 24h-old thread. | Standard |
| F-16 | ⬜ | 02 | **Run the live model evals with credentials:** `npm run test:persona -- --live` (needs `AI_GATEWAY_API_KEY` or `VERCEL_OIDC_TOKEN`), on both haiku-4.5 and sonnet-5. Confirm the ~43 bot-question phrasings produce **zero humanity claims**, the mirror bars hold, and no bot-tell fires. | Without credentials these are lint functions over a hand-written corpus — they prove the detectors and the corpus agree with each other, not that the model behaves. The spec calls them hard CI gates. | **HARD BLOCKER** before the flag |
| F-17 | ⬜ | 02 | **Native-speaker read of 20 real drafts from the shadow week.** Score for the twelve bot tells, apni/tumi handling, and whether the identity-disclosure sentence reads warm rather than legalistic. | Automated evals prove the detectors, not that the output is good. Only a Bangladeshi speaker can judge whether Nova sounds like a shopkeeper or like software. | Standard |
| F-18 | ⬜ | 02 | **Set `inbox.persona` per pilot tenant** (addressForm, emojiLevel, dearAllowed, disclosureMode, personaLabel, nightMode). Note `personaLabel` defaults to the registry signature — that is the name a customer is told when they ask if they are talking to a bot. Decide what values founders may set: "Rima" reads as a woman named Rima. | A brand/product judgement, not a code one. Also worth a legal/product sign-off that the three approved disclosure sentences satisfy your BD bot-disclosure posture, and that you accept `on_ask` is not merchant-disableable. | Standard |
| F-19 | ⬜ | 02 | **Confirm no migration is needed for module 02** — the integrator asserts module 01's `20260725034728_nova_front_office_01_pipe` covers every column used. Worth one look at the deployed Railway schema for `InboxOutbound.metaMids`, `InboxMessage.purpose`, `InboxConversation.escalatedAt` before the first real send. | Local Postgres only, by design. Same class of risk as the 2026-07-01 P0. | **HARD BLOCKER** |
| F-20 | ⬜ | 02 | **Decide the founder-prompt trim** (or accept the ceiling). `evals/isolation` fails at 3085/2500 tokens for the FOUNDER register — pre-existing, and the module-02 split moved founder text *out* of customer sessions without removing it from founder ones. The departments list (~950 chars) largely duplicates 40-routing's generated intent table. | A product call about what Nova's founder-facing prompt should contain, not an engineering one. Deliberately not papered over by raising the ceiling. Module 03 did NOT worsen it — the number is byte-identical (3085). | Standard — does not block the flag |
| F-21 | ⬜ | 03 | **Confirm the module-03 migration deployed to Railway** (`20260725101526_nova_front_office_03_identity`) and the app came up clean. It creates `NovaPromise`, adds five identity columns to `InboxConversation`, `StorefrontLead.conversationId` and three indexes — **and runs a hand-written data statement that rewrites existing `CustomerChannel` sms addresses** into normalized form, dropping raw duplicates where a normalized row already exists. | Applied to local Postgres only, by design. The data statement touches existing production rows, which per `RELEASE_CHECKLIST.md` and the 2026-07-01 P0 is exactly the class to watch. It was restructured once already after review found a version that would have violated `@@unique([tenantId, kind, address])` and aborted mid-deploy — reviewed statically, **never executed against a real Postgres**. Note a second, unrelated migration (phone normalization, `bbf2c43`) landed in the same window; check the ordering. | **HARD BLOCKER** — a failed migration takes the API down |
| F-22 | ⬜ | 03 | **Run module 03's gate demo steps 1, 3 and 5 as a non-builder** on a clean staging store: state a phone matching a seeded Customer → the next reply references the real order history with no full phone anywhere in the transcript; ask something Nova must check on → a `NovaPromise` row appears, and past `dueAt + grace` a broken-promise Decision does; fire the data-deletion callback → messenger spoke + psid memory gone, Customer + orders intact (verify by direct DB read). | The §15 gate wants someone who didn't build it, a real Page, a real second Meta account and real Bangla. Steps 2 and 4 are **not runnable today** — see F-23/F-24. For step 3 the sweep is nightly at 03:00 Dhaka with a 12h default grace, so the script must seed a past `dueAt` or force the tick. | **HARD BLOCKER** — module gate |
| F-23 | ⬜ | 03 | **Decide whether the MEDIUM proposal rung ships or is deferred.** Gate step 2 (a stranger claims to be a customer by name → Nova asks for the last 2 digits → wrong answer → warm fallback with zero leaked digits) **cannot run**: `proposedCustomerId` is written only by `PATCH /conversations/:id`, and no tool or StoreClient method sends that field, so Nova can never author a proposal and the digit check never has a candidate. The question scripts, the one-attempt burn and the zero-leak fallback are all built and tested behind it. | A product call: ship a propose path — which means deciding what evidence is enough to *ask* — or defer the rung and drop step 2 from the gate. Not a gap I should close by guessing a threshold; the whole rung exists to avoid guessing about identity. | **HARD BLOCKER** — gate step 2 is unrunnable until decided |
| F-24 | ⬜ | 03 | **Confirm a linked customer is actually visible in the merchant Inbox, and decide who builds it if not.** Gate step 1 says the founder sees the linked customer there. The backend link and the masked 360 work, but `GET /api/meta/conversations` returns only id/platform/senderId/senderName/senderAvatar/lastMessageAt/lastMessage — **no `customerId`, no customer name** — so a linked thread renders identically to an unlinked one. Module 03 touched zero merchant UI, correctly: module 10 owns that surface. | Whether this blocks module 03's gate or moves to module 10 is your call. Flagging it because the gate script as written cannot pass without it, and because a founder currently cannot see the thing the whole module exists to provide. | Standard — decide the owner |
| F-25 | ⬜ | 03 | **Red-team the identity boundary on a live session.** (a) Set a Facebook display name to an injection string and confirm it reads as a customer *claim*, not shop fact — the fence is code-proven and reproduced in a test, but only a live model proves it cannot be talked around. (b) Confirm Nova never recites a masked phone back as though it were the full number. (c) Confirm a failed digit check leaks nothing in the actual Bangla reply, not merely in the API response. | The tests prove the guards fire; only a live session proves the model cannot route around them. This is the module's own named worst case — a wrong-person link showing one customer another's order history. | **HARD BLOCKER** before autonomous on a real store |
| F-26 | ⬜ | 03 | **Watch the first nightly sweep on a tenant with real volume.** `promise_sweep` / `identity_merge_sweep` / `conversation_distill` run server-side inside an explicit 120s transaction (raised from Prisma's 5s default, which would otherwise have aborted every night forever). Confirm they complete and that `nova.sweep.timeout.<kind>` stays at zero. | Every sweep test mocks Prisma, so no real transaction timeout is observable in CI. The failure mode is quiet: the retry re-runs the identical batch, so a timeout cannot converge on its own. | Standard — but check it the first week |
| F-27 | ⬜ | 03 | **Judge the merge Decision card against real duplicate records.** It now shows head *and* tail (`017123…78`) after review found both sides rendering identically as `017123…`, leaving the founder unable to tell two people apart while approving a merge that deletes a Customer and repoints their orders, with no undo. | Whether a human can actually distinguish the two records from that card is a judgement about a person reading a UI, and it is the only gate protecting a not-undoable financial rewire. | **HARD BLOCKER** before `merge_customer_records` is approved on real data |
| F-28 | ⬜ | 03 | **Decide who wires a fact source for `conversation_distill`.** The job ships, is scheduled, and honestly does nothing: extraction is a language judgement and the sweep lane is deliberately model-free, so it reports `distilled: 0, factSource: null` and counts `inbox.memory.distill_unavailable`. The write half (guarded, capped at 3, correctly keyed, 18-month TTL) is built and tested — wiring a source is one call. | Until it is wired, the 360's `preferences[]` only ever carries what a founder wrote by hand, so "Nova remembers this customer" is thinner than the module doc implies. Not broken, but not the promised capability either. | Standard |
| F-29 | ⬜ | 04 | **Confirm the module-04 migration deployed to Railway** (`20260726090000_nova_front_office_04_lifecycle`) and the app came up clean. It creates `CustomerJourney` + `JourneyTransition`, adds `NovaInbox.journeyAt` and `Order.courierStatusAt` — **and backfills `journeyAt` on every existing row**, so the historical commerce backlog is skipped rather than replayed into fresh journeys on deploy. | Applied to local Postgres only, by design. The backfill touches every existing `NovaInbox` row; per `RELEASE_CHECKLIST.md` and the 2026-07-01 P0 that is the class to watch. Note THREE migrations are now queued unapplied (module 03's, the phone normalization, and this one) — check the ordering. | **HARD BLOCKER** — a failed migration takes the API down |
| F-30 | ⬜ | 04 | **Watch the first real `journey_sweep` and the first busy claim tick.** The nightly sweep runs at 04:30 Asia/Dhaka inside a 120s transaction; the job claim now runs inside 30s while draining events, running the journey pass, expanding defs, leasing sweeps and re-checking follow-ups. Confirm `nova.sweep.timeout.*` and `nova.claim.timeout` both stay at zero on a store with real volume. | Every test here mocks Prisma, so no real transaction timeout is observable in CI. Module 03 shipped this exact class of bug (Prisma's 5s default) and it was only caught by review, not by a test. The failure is quiet: the retry re-runs the identical batch, so a timeout cannot converge on its own. | **HARD BLOCKER** — silent nightly failure otherwise |
| F-31 | ⬜ | 04 | **Verify the quiet-hours and touch-budget backstop under a real backlog.** Review found follow-ups past the re-check ceiling were being leased UNCHECKED — Nova messaging customers at 3 a.m., and the documented `inbox.maxProactiveTouchesPerWeek: 0` kill switch leaking sends every tick. Fixed structurally (the candidates query cannot lease an unexamined follow-up), but please confirm on staging: back up >50 follow-ups, force a claim during quiet hours, and check that ZERO were sent. Then set the cap to 0 and confirm nothing proactive goes out at all. | The fix is machine-proven against the real router, but the failure mode is customer-visible and reputational (a 3 a.m. message, or a Meta policy strike), and the kill switch is what you would reach for in an incident. It should be confirmed by someone other than the person who fixed it. | **HARD BLOCKER** before any proactive send on a real store |
| F-32 | ⬜ | 04 | **Run module 04's gate demo as a non-builder** on a clean staging store: message the page and confirm the journey shows `inquirer` with one transition row; place a storefront order with the SAME phone in a different format (`+880…`) and confirm the journeys MERGE into one row at `ordered`; ask Nova to follow up, see the commitment listed, cancel it from the merchant surface; schedule another and reply as the customer before it fires (it should read `cancelled:customer_replied`, nothing sent); force-fire one during quiet hours (it should re-lease to morning); run `journey_sweep` against a 45-day-silent seeded journey and confirm `dormant` with nothing sent. | The §15 gate wants someone who did not build it, a real Page and a real second Meta account. The merge step is the one to watch — it is where P-01's multi-identifier resolution actually earns its keep. | **HARD BLOCKER** — module gate |
| F-33 | ⬜ | 04 | **Confirm every founder-visible `journey.*` number equals `SELECT count(*)` over `JourneyTransition` for the same window.** The module's own gate step 7. Note one counting contract that is easy to get wrong and cannot be fixed later, because the table is append-only: a confirm that arrives behind its courier update lands as a SAME-STAGE marker row, so module 09 must count `eventType = 'order.confirmed'`, **not** `toStage = 'confirmed'`. | Only a human comparing the rendered tile against the database can prove "every founder-visible numeric traces to ledger rows" actually holds. | Standard — but do it before module 09 renders anything |
| F-34 | ⬜ | 04 | **Decide whether `do_nothing` should be counted at all, and how.** `journey.silences_chosen` ships NOT IMPLEMENTED on purpose: `do_nothing` is the only NBA candidate declared by calling nothing, so from the channel a deliberate silence is indistinguishable from a turn whose tool call failed validation or that found nothing answerable. Counting it would be a guess presented as a metric. Making it real needs the model to DECLARE the choice (a tool call, or a field on an existing one). | A product call about whether "Nova chose restraint" is worth a round-trip to measure. I will not infer it — that is the one thing this project does not do. Requirement #5 asks for proof of restraint, so leaving it unmeasured is a real gap, not a non-issue. | Standard |
| F-35 | ⬜ | 04 | **Native-speaker read of the NBA-driven turns during the shadow week.** Rule 18 now tells the model the candidate list is a constraint, that ineligible rows carry reasons it may steer by, and that `do_nothing` is a legitimate answer. Confirm it does not read reason codes out loud to customers, does not sound like it is consulting a menu, and that "won't disturb you again today" lands warmly rather than robotically. | Automated evals prove the rule renders and the detectors fire. Only a Bangladeshi speaker can judge whether a model steering by machine-readable eligibility still sounds like a shopkeeper. | Standard |
| F-36 | ⬜ | 08 | **Confirm the module-08 migration deployed to Railway** (`20260726160000_nova_front_office_08_handover`) and the app came up clean. Purely additive: 5 columns on `InboxConversation` plus one composite index, zero backfill. **FOUR migrations are now queued unapplied** (module 03's, the phone normalization, module 04's, this one) — check the ordering before deploying, and note module 04's backfills every row of `NovaInbox`. | Applied to local Postgres only, by design. Four unapplied migrations is more than any previous deploy in this phase, and `RELEASE_CHECKLIST.md` exists because of the 2026-07-01 P0. | **HARD BLOCKER** |
| F-37 | ⬜ | 08 | **Verify tap-send end to end on staging, as a non-builder.** Escalate a real thread, open the card, tap SEND with a small edit, and confirm the bubble actually arrives in Messenger — then check the ledger row says the reply was sent and does NOT contain "the founder took the thread over". | Review found this delivered NOTHING while reporting success, 100% of the time, and wrote a false statement about the founder into the audit trail. It is fixed and now covered end to end by a test, but it is the module's headline flow and the failure was invisible from inside the system. Someone other than me should watch a real bubble land. | **HARD BLOCKER** |
| F-38 | ⬜ | 08 | **Run the module-08 gate demo as a non-builder** on a clean staging store with a real Messenger test page: verify T0 Shadow shows and T1+ are disabled with an honest reason; send "আসল মানুষ আছে?" and confirm the Bangla holding line arrives plus a priority-1 card on the Desk, the Support room, and as a toast; open the card and check the brief; tap SEND with an edit; type in `/inbox` while Nova is composing and confirm the ledger shows a blocked `concurrency:founder_active`; reply from the Meta Business Suite app and confirm the thread goes founder-owned within ~5s and the card settles; hand back with a promise quick-pick and confirm Nova resumes without re-greeting. | The §15 gate wants someone who did not build it, a real Page, a second Meta account and real Bangla. Step 5 (the phone reply) should already have worked before module 08 — the doc claims otherwise and the doc is wrong. | **HARD BLOCKER** — module gate |
| F-39 | ⬜ | 08 | **Prove the lock holds: zero customer-visible sends with `actor:'nova'` on any locked thread across the whole demo.** The module's own measurable check, and it is a SQL assert, not a vibe. Run it after F-38 and keep the output. Watch specifically for a double reply — the founder answering on their phone while a Nova send is mid-flight. | The residual window between a Graph send and its echo landing is real and documented; only a live run tells you how wide it is in practice. This is the module's named worst customer-facing failure. | **HARD BLOCKER** before autonomous on a real store |
| F-40 | ⬜ | 08 | **Decide whether Design 7's auto-hand-back ships, and who builds it.** After a founder taps SEND on an escalation card the thread stays founder-owned — Nova does not resume until an explicit release. `ctx.keepThread` is plumbed through the approve path and read by nobody. Today's behaviour is safe (Nova stays silent rather than barging back in) but it means every escalation needs two founder actions instead of one. | A product call about how much ceremony a hand-back should take, not an engineering gap. I did not guess: the end-to-end test pins current behaviour, so whoever builds it has to change that assertion deliberately. | Standard |
| F-41 | ⬜ | 08 | **Native-speaker read of the holding lines H1–H6 and the SLA line S1 in all three scripts** (Bangla, Banglish, English). Confirm none names a wait time, none says bot/AI/system, and that H3 (anger) reads as a genuine apology rather than a form letter. Then check the escalation brief reads like something a busy founder can act on in ten seconds. | Automated evals prove the templates render and the banned-word detectors fire. Only a Bangladeshi speaker can judge whether a holding line calms an angry customer or annoys them further — and that is the entire job of H3. | Standard |
| F-42 | ⬜ | 08 | **Watch the first `inbox_sla_sweep` on a tenant with real escalations.** It runs every 10 minutes on the server lane. Confirm it sends exactly one S1 per escalation and never a second, never fires during quiet hours, never fires on a thread the founder answered externally, and never selects a voluntary takeover. | Every sweep test mocks Prisma, so no real transaction or real timing is observable in CI. Modules 03 and 04 each shipped a transaction-budget bug on this lane that only review caught. | Standard — but check it the first week |
| F-43 | ⬜ | 05 | **Confirm the module-05 migration deployed to Railway** (`20260726200644_nova_front_office_05_selling`) and the app came up clean. Purely additive DDL — three columns on `Order` + one index, `OrderItem.variantId`, `StorefrontLead.recoveryMessage`, and the new `TenantPolicy` table. **Not one row-touching statement**, no backfill. **FIVE migrations are now queued unapplied** (03's, the phone normalization, 04's, 08's, this one) — check the ordering, and note 04's backfills every row of `NovaInbox`. | Applied to local Postgres only, by design. Five unapplied migrations is the most this phase has ever carried, and `RELEASE_CHECKLIST.md` exists because of the 2026-07-01 P0. | **HARD BLOCKER** |
| F-44 | ⬜ | 05 | **Verify the duplicate-order fix on staging under real concurrency.** Fire 4 simultaneous `POST /api/v1/store/orders` carrying one `Idempotency-Key`, then `SELECT count(*) FROM "Order"` for that `novaActionId`. Expect exactly 1 order, one 201 and three 409 `IDEMPOTENCY_IN_PROGRESS`. Repeat against `POST /discounts`. | Reproduced against local Postgres and fixed with an integration test that fails without the fix, but this is the failure where a **customer is charged COD twice for one basket**, and it lived in a shared wrapper used by nine routes. It should be confirmed by someone other than the person who fixed it, on a database with real latency. | **HARD BLOCKER** before any chat order on a real store |
| F-45 | ⬜ | 05 | **Set `inbox.maxDiscountPct` per pilot tenant, and decide whether it should equal the store-wide `maxDiscountPct`.** Both seed to 15 so nothing changes on landing day, but they are two independent keys: the store-wide one bounds a founder's dashboard/campaign coupon, the inbox one bounds what Nova may give away mid-conversation to close one cart. **Also decide what happens for taka discounts** — there is no taka-denominated ceiling anywhere, so a FIXED or free-delivery offer has nothing to check against and always drafts. | A margin decision, not an engineering one. Flagging it hard because for eighteen hours this ceiling was enforced **nowhere**: the agent check sat behind a switch that ships off, and no server checked it at all, while the tool description told the model it was server-enforced. It is now enforced in both places — but the *number* is yours. | **HARD BLOCKER** before `offer_chat_discount` on a real store |
| F-46 | ⬜ | 05 | **Run module 05's gate demo as a non-builder** on a clean staging store with a real Messenger page: haggle → confirm Nova declines once before offering anything; accept an offer → a Decision card appears, approve it → the coupon exists and validates against a real cart; walk a full order to the confirm bubble → confirm the customer is told **"shop owner ektu porei confirm korben"** and NEVER "order hoye geche", because `inbox.orderAuto` is false; approve the card → the order exists with the right variant, the stock moved, and the by-Nova chip on the orders list opens its receipt; send a payment-slip photo → confirm Nova says the word *claim* and never says the payment was received. | The §15 gate wants someone who did not build it, a real Page and real Bangla. The confirm-bubble step is the one to watch: review found the playbook's only worked closing script was the one this build can never produce. | **HARD BLOCKER** — module gate |
| F-47 | ⬜ | 05 | **Decide when (and whether) module 06 unifies the two order paths.** `routes/orders.js` still has its own order-create path; `lib/orderCreate.js` is Nova-only (FD-1) and was extracted from the *storefront* path instead, because the merchant one is variant-blind, decrements stock after the response, and accepts client-supplied `discount`/`paid`/`qty`. No parity test exists, and one written today would mostly assert deliberate differences. | A scope call. The two paths can drift, and the merchant one is the weaker of the two — but unifying them is a blast radius across every merchant order, not a Nova change. | Standard — but decide before module 06 touches orders |
| F-48 | ⬜ | 05 | **Watch the first real `cart_sweep` on a tenant with abandoned carts AND live threads.** Confirm no customer gets both an email/SMS from the sweep and an in-thread nudge for the same basket. The split is keyed on `conversationId`, which the sweep tool did not return until this module — so every cart with a thread was being worked by both lanes. | The fix is test-pinned (including the absent-vs-null distinction), but "no customer was contacted twice" is a claim about real customers on a real day, and the double-contact is customer-visible and reputational. | **HARD BLOCKER** before the cart sweep runs on a real store |
| F-49 | ⬜ | 05 | **Fix the dollar amount in `agent/skills/cart-recovery.md`, or confirm it is intentional.** The discount rule reads "only for carts over $80, only up to 10%". Every number in this product is taka. As written it is a 100× wrong threshold that predates module 05 and was left untouched here rather than silently reinterpreted. | Guessing the taka figure would be inventing a merchant's discount policy. ৳80 and ৳8,000 are very different rules and only you know which was meant. | Standard — but it is wrong today |
| F-50 | ⬜ | 06 | **Confirm the module-06 migration deployed to Railway** (`20260727000743_nova_front_office_06_cases`) and the app came up clean. ONE new table (`NovaCase`) plus four indexes, no column added to any existing model, and **not one row-touching statement** — no UPDATE, no INSERT, no backfill. **SIX migrations are now queued unapplied** (03's, the phone normalization, 04's, 08's, 05's, this one) — check the ordering, and note 04's backfills every row of `NovaInbox`. | Applied to local Postgres only, by design. Six unapplied migrations is the most this phase has carried; `RELEASE_CHECKLIST.md` exists because of the 2026-07-01 P0. | **HARD BLOCKER** |
| F-51 | ⬜ | 06 | **Watch a real parcel move through the fixed courier webhook.** Ship one order, then check `Order.courierStatus` and `courierStatusAt` actually change on EVERY scan — including two scans that map to the same Dakio status (Pathao `Picked Up` → `In Transit`, or Steadfast `pending` → `hold`). Then confirm a `NovaInbox` `order.updated` row exists per scan. | This was a live bug affecting every order you ship, it predates Nova entirely, and `updateOrderByTracking` had **no test of any kind** before this module. It is now covered end to end against a real Postgres, but the failure was invisible from inside the system for as long as the function has existed, so it is worth watching one real parcel. | **HARD BLOCKER** — the stagnation clock and every delivery case depend on it |
| F-52 | ⬜ | 06 | **Decide `inbox.addressEditAuto`, and accept the cost either way.** It ships FALSE, so a customer correcting their own address in chat produces a Decision rather than an immediate fix. The reason is that the address is where a COD parcel worth real money goes and "whoever is typing in this thread" is not the same claim as "the person who placed the order" — a redirect is a fraud shape. Turning it on removes a founder tap and removes that check. | A fraud-versus-friction call on your own customers, not an engineering one. Flagging it hard because this verb first shipped as an unconditional allow and the eval suite is what caught it. | **HARD BLOCKER** before `update_order_contact` runs on a real store |
| F-53 | ⬜ | 06 | **Run module 06's gate demo as a non-builder** on a clean staging store with a real Messenger page: ask "amar order ta kothay?" → Nova quotes the real step and NO delivery date; ask again from a second account about the SAME order → one case, one card, and the second asker is answered from the case; let a parcel sit past the stagnation window → a `delivery_stuck` case opens by itself; approve the courier flag → confirm the ledger says nothing was sent to the courier; walk a pre-dispatch confirm and an address change → confirm the address change RESETS the confirmation and re-prices when the district changes. | The §15 gate wants someone who did not build it, a real Page and real Bangla. The two-accounts step is the one to watch — it is where the `activeKey` claim earns its keep. | **HARD BLOCKER** — module gate |
| F-54 | ⬜ | 06 | **Confirm nothing celebratory is claimed for a rescue that failed, and no apology goes to a customer whose delivery was fine.** Watch the first week of `delivery_stuck` cases on a store with real volume: every case that opened should correspond to a parcel that genuinely stopped moving. | The stagnation clock was reading a column that was never being written (F-51), so before this module a moving parcel could look frozen. The fix is test-pinned, but "no customer got an apology they did not need" is a claim about real customers on a real week. | Standard — but check it the first week |
| F-55 | ⬜ | 06 | **Decide whether the `courier-refresh` route ships, and who owns it.** Not built. `GET /orders/:id/courier/status` does NOT persist `courierStatus`, implements **Steadfast only** (RedX and Pathao answer null), and does a live HTTP poll — which inside `w()`'s 60-second stale-claim window is exactly the shape that lets a second request take over a live claim. Wrapping it as the doc specifies would return a fresh status that never lands and would not reset the stagnation clock. | Three separate problems, each a real decision: persist-or-not, the two unimplemented providers, and where a slow external call is allowed to live. I did not paper over them with a route that looks like it works. | Standard — decide before module 07 leans on it |
| F-56 | ⬜ | 06 | **Note that movement 4 (failed-attempt rescue) is NOT built, and decide whether it should be.** No courier sends a "failed attempt" status — the only near-match is Steadfast `hold`, which now reaches the journey reducer for the first time (it could not before this module) but which nothing consumes. The doc builds a whole rescue flow on a signal that does not exist. | Whether a `hold`-only rescue is worth having is a product call about one provider's semantics. Claiming rescue coverage across three couriers on the strength of one string would have been the dishonest option. | Standard |
| F-57 | ⬜ | 07 | **Re-test every module-06 delivery flow, because none of it could run until now.** Module 06's five verbs were built into every table that takes a verb — `ActionType`, executors on both sides, risk classes, guardrails, duties, a 41-check eval suite — and never got tool files, and nothing minted an action row for them either. So the case system had **no entry point at all**: zero `NovaCase` rows, of any kind, could be created in production. F-53's gate demo could not have passed. Module 07 built the four customer-plane tools (`open_case`, `confirm_order_intent`, `update_order_contact`, `cancel_order_from_chat`) and added `check:reachability` to CI so a verb can never again ship without a way in. | 1852 dakio-api tests and 778 inbox checks were green the whole time, because they call the routes and executors DIRECTLY — the layer below the missing one. Nothing asserted reachability, so nothing noticed. **Anything you believe module 06 delivered should be re-verified rather than assumed**, and F-53 should be run from the top. | **HARD BLOCKER** — supersedes F-53 |
| F-58 | ⬜ | 07 | **Decide who builds the producers for `courier_intervention` and `restock_check`, or retire the lanes.** Both are real `JobKind` members with real priorities and real prompt templates, and **nothing anywhere enqueues either one**. `case_update` does have a producer (`drainCaseUpdated` ← `emitCaseUpdated`) but it is gated on an open case, so it was dead by transitivity until F-57 landed. `flag_courier_issue` is the verb `courier_intervention` exists to raise, and it is declared in `check:reachability`'s exemption list precisely because its lane has no producer — that declaration is the tracking record. | Not a bug I could fix inside module 07's scope: a sweep that decides *which* stuck parcels deserve a founder's phone call is a product decision about how much noise a founder will tolerate, and guessing the threshold would repeat module 06's mistake in the other direction. | Standard — decide before relying on courier intervention |
| F-59 | ⬜ | 07 | **`computeRtoSignals` has no caller.** `src/lib/customerRisk.js:149` computes six RTO risk rules over a customer's history, is fully unit-tested, and **nothing in production calls it**. It was built in module 06 alongside `inbox.rtoShadowThreshold`, which is likewise read by nothing. Decide where the signal belongs — the NBA block, the order-create path, or the pre-dispatch confirm — before another module builds on the assumption that RTO risk is already flowing. | Same class as F-57, found by the same sweep. Wiring it somewhere arbitrary would put a fraud-adjacent score in front of a founder without anyone deciding what it should change. | Standard — but it is dead code today |
| F-60 | ⬜ | 07 | **Decide whether the reorder-window follow-up ships, and who unblocks it.** Not built. Three independent route refusals stand in the way: `POST /followups` hard-422s without `scheduledByActionId`, the 24h-window pre-filter refuses any `dueAt` past `windowExpiresAt` (and a reorder window is always weeks out), and the spec's dedupeKey is not the real namespace, so a row written its way is invisible to the reply-rate prior. Building it means a raw `prisma.novaJob.create` that also bypasses supersede, chain-cap and window checks. **And the `skipped_window` receipt it is meant to produce has nowhere to render**: `GET /api/nova/followups` filters `status in ['due','leased']`, so a skipped job vanishes, and the card carries no brief line. | The eligibility half is built and live — the window, the priors, the real product name, `recommend_product` ranked first. What is missing is the proactive send and a founder surface for its receipt, and each of the four blockers is somebody's decision rather than an oversight. | Standard — decide before promising proactive reorder nudges |
| F-61 | ⬜ | 07 | **The weekly dormant digest is not built, on purpose.** D7 specifies an aggregation over dormant customers; nothing consumes it, and the `night_ops` pass that would read it does not exist. | Building an aggregation with no reader is the exact mistake F-57 documents. If you want the digest, the consumer is the thing to specify first. | Standard |
| F-62 | ⬜ | 07 | **The complaint severity ladder is prompt-side only — it has no deterministic backstop.** D3 specifies it against `negSentimentStreak` and `messageAssessmentSchema`, which are **module 11's** and have not shipped. There is no sentiment column anywhere in the schema and `InboxConversation.urgency` does not exist. The escalation lexicon, the handover path and the §14 copy are real; the "backstops (server-side, not prompt-side)" the doc promises are not. | A founder reading module 07's spec would reasonably believe an angry customer is caught by a server rule. Today they are caught by instructions, which a model can be talked out of. Worth knowing before the shadow week. | **HARD BLOCKER** before relying on automated complaint handling |
| F-63 | ⬜ | 07 | **`reuseAddressFromOrderId` is not built and needs a respec first.** `Order` has **no address columns at all** — no `customerAddress`, `customerCity`, `customerDistrict`. The address lives on `Customer`, written at order-create. So the field cannot "copy address/city/district from that order" as D5.2 specifies; it would resolve `order.customerId → Customer`, which for a repeat buyer is already the address `createOrder` would use anyway. | The genuine value is much narrower than the doc claims — a *permission signal* ("reuse the stored one"), not a copy. Building it to the spec would have shipped a field whose name describes something the data model cannot do. | Standard — respec before building |
| F-64 | ⬜ | 07 | **Decide whether the aftersales duty toggles should actually bind.** Every module-07 reply — complaint, review ask, win-back, reorder nudge — runs under `support.inbox_replies` (minLevel 2). Pausing `support.complaint_resolution` (minLevel 3), `support.refund_processing` (4), `support.replacement_shipments` (4), `support.review_responses` or `sales.winback_campaigns` **changes nothing about what Nova sends.** | A founder who switches off "complaint resolution" expecting complaint replies to stop will get exactly the same behaviour. That is a trust problem, not an engineering one — and rewiring the duty a reply runs under is a decision about what a toggle promises, so I did not guess. | **HARD BLOCKER** before the duty screen is shown to a pilot merchant |
| F-65 | ⬜ | 09 | **Confirm BOTH module-09 migrations deployed to Railway** (`20260727230000_nova_front_office_09_ledger`, `20260727234500_nova_front_office_09_achievements`) and the app came up clean. Purely additive: one nullable column, five indexes, one new table, one FK — **not one row-touching statement** across either. **NINE migrations are now queued unmerged.** | Applied to local Postgres only, by design. Nine is the most this phase has carried and `RELEASE_CHECKLIST.md` exists because of the 2026-07-01 P0. | **HARD BLOCKER** |
| F-66 | ⬜ | 09 | **Watch the first real `inbox_attribution` sweep, and check one order end to end.** It runs 05:00 Asia/Dhaka, after `journey_sweep`. Take one chat order that DELIVERED and one that came back: the first should flip to `revenueBasis:'measured'` at the order's total, the second to zero. Then export the ledger and confirm you can rebuild `/nova/home`'s `revenueToday.measured` from the `record:"activity"` lines. | This is the first thing in the product that decides whether a revenue number a founder reads is real. It is covered by 14 integration tests against real Postgres, but "the right orders were reconciled on a real night" is a claim about real data. | **HARD BLOCKER** before any revenue figure is shown as measured |
| F-67 | ⬜ | 09 | **Five scorecard tiles are deliberately NOT built, and the reasons are engineering ones you may want to overrule.** `median_first_reply_seconds` and `resolved_without_handover_pct` need per-conversation 6h-silence session math — a scan over every message in the window inside a 120s budget shared with every other nightly sweep. `carts_recovered_via_chat` counts a provenance flip that nova-ai's `reflection` job writes in a DIFFERENT format, so it would report zero forever while looking implemented. `ad_replies_handled` needs a Meta referral payload nothing persists. | A tile that cannot be computed honestly is worse than a missing one — a founder reads a zero as "this did not happen", not "nobody measured". If you want the first two, the cost is a dedicated indexed pass, not a line in this sweep. | Standard |
| F-68 | ⬜ | 09 | **The ~40-key metric registry (`journey.*`, `case.*`, `inbox.promise.*`, `inbox.identity.*`, `inbox.disclosure.*`) is not built.** It has no storage beyond the 7 keyed tiles that shipped, and no reader. | Same reason as F-61: building an aggregation with no consumer is the mistake this phase keeps making. If you want these, the consumer is the thing to specify first. | Standard |
| F-69 | ⬜ | 09 | **An ungraded department room still renders INVENTED numbers.** `useNovaRoom.js:9-15` falls back to the mock fixture in `novaData.js` for grade/now/next/memo/**score** until a night-shift grade row exists. So a real tenant with real metrics but no grade sees fabricated scorecard values. Module 09's achievements deliberately do NOT do this (real or absent), but the card above them still does. | Pre-existing and not module 09's to change unilaterally — removing the fallback makes every ungraded room look broken, which is a product decision about what an un-set-up room should say. Flagging it because this module is the one that made the rest of the room honest. | **HARD BLOCKER** before a pilot merchant sees a department room |
| F-70 | ⬜ | 09 | **Five job lanes still have no producer**: `night_ops`, `morning_report`, `pulse`, `weekly_strategy`, `reflection`. None is in `PLATFORM_JOB_DEFS`, and `novaJobs.js:626-628` says a def-only lane "ships dark". Separately, `conversation.taken_over` events have no drainer and accumulate `processedAt: null` forever — `novaEvents.js:83-85` names module 09 as the owner of that leak, and it is a queue that only grows. | Module 09 added its own producer rather than inheriting the gap, so the attribution sweep runs. The other five are somebody's decision about which nightly lanes should exist at all — and the morning report is the one a founder would most expect to be running. | **HARD BLOCKER** before relying on the morning brief |

---

---

## Where the code is (branch split)

From 2026-07-23, Nova work goes to a **`develop`** branch in every repo it
touches, so `main` stays reviewable and production-safe until you merge.

| Repo | `main` has | `develop` has |
|---|---|---|
| `nova-ai` | phase 06 chunks A–H + D2 (7 commits) | everything from chunk I onward |
| `dakio-api` | phase 06 backend (4 commits, **includes a migration**) | everything from chunk I onward |
| `dakio-merchant` | untouched by Nova work so far | all Nova merchant UI |

⚠️ **The dakio-api phase-06 commits are already on `main`** — pushed under the
standing commit-and-push instruction, before the branch policy existed. That
includes migration `20260722120000_nova_stage0_spine`. If Railway auto-deploys
`main`, it has already been applied there; **H-3 is how you confirm it.**
Nothing has been pushed to `main` in any repo since the policy took effect.

`nova-ai` and its demo seeds have no production surface, which is why D2
(demo/eval data only) was allowed to finish on `main` before the split.

---

## Judgement calls I made that you may want to revisit

These were mine to make to keep moving, but they are the kind of call you might
decide differently. Nothing here is blocking.

| # | What | My reasoning |
|---|---|---|
| J-1 | **The OTP dev bypass is now pinned by a test rather than flagged as a bug.** In any environment where `NODE_ENV` is not exactly `"production"`, a failed verification email returns the OTP inline (HTTP 200) and leaves the `EmailOtp` record live, instead of failing closed. `src/routes/auth.js:104-118`. | Judged intentional and internally consistent rather than a leak: the SUCCESS path already returns `devOtp` in non-production, so the failure branch discloses nothing the success branch doesn't in the same environment; and deleting a record whose code *was* delivered (inline, in the response) would break the very flow the bypass exists to enable. Git backs this — the test predates the bypass (`1954c8e`), and `d0bd276` added it deliberately under its own feature name. `NODE_ENV=production` is load-bearing, documented deploy config. **But** it is a security-shaped guarantee resting on a single env var, so if you'd rather it fail closed everywhere, that's a one-line inversion — say the word. The test now pins BOTH paths, so neither can drift unnoticed. |

## Standing context for whoever picks this up

- **Local DB is safe to seed** (`localhost:5433`). Railway prod URL is line 1 of
  `.env` and is **commented out**; dotenv is first-wins. Verify that before any
  migration or seed command.
- **Receipt rule (PRD §16.2):** a write missing its receipt is a failed write.
  The API enforces it with a 422 on every status, including `blocked`.
- **Attribution is metadata, never authority.** A door row with a null
  `novaActionId` is still a valid row; the NovaAction ledger row is the truth.
- **Tenancy comes only from verified auth**, never from model input.
